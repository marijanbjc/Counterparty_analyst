# Доработки и известные проблемы

Что нужно поправить и почему. Каждая запись содержит воспроизведение,
следствие и предлагаемую правку.

Реестр вычищен 5 сентября 2026: закрытые записи убраны в §3, нумерация сквозная.
Формулировка «проверено» означает, что состояние подтверждено запуском кода,
а не прочтением.

---

## 1. Направления работ

| Приоритет | Направление | Записи |
|---|---|---|
| ~~1~~ | ~~Структура ответа, риск в чате, сравнение, сводная таблица~~ | **сделано, см. §3** |
| ~~2~~ | ~~Бюджеты, профиль, сравнение, качество списков, графики~~ | **сделано, см. §3** |
| 3 | Инструменты по выборке контрагентов | 11 |
| 3 | Пересборка архитектурной документации | 12 |
| — | Решение по поставщику LLM (OpenRouter) | 13 |

**Ограничения ключа Groq, с которыми живём:** 8 000 токенов в минуту
и **200 000 в сутки** — примерно 50–60 полных разборов в день на всю команду.

---

## 2. Открытые записи

### 11. Инструменты по выборке контрагентов

**Статус.** Спроектированы и отложены (`mcp_architecture.md` §5).
`find_similar_contractors` реализован и работает, `search_contractors` исключён
из архитектуры.

**Причина отказа.** Инструмент возвращает модели названия и ИНН реальных компаний,
и через ответ агента состав базы может утечь пользователю.

**Что потребуется, если внедрять.**

* Пересмотр роутера и первого хода: сейчас развилка строится по числу ИНН
  в сообщении, а для поиска нужен вопрос «проверить конкретного или подобрать».
* Решение по утечке состава базы — вопрос к кейсодателю: справочник ли это
  из внешних источников или в нём есть данные, которые клиент видеть не должен.

**Приоритет:** дополнительная функциональность, не входит в основной контур.

---

### 12. Пересборка архитектурной документации

**Суть.** Документы писались итеративно и накопили расхождения с кодом:

* `architecture.md` — исходный, содержит решения, от которых отказались
  (LangGraph, `resolve_contractor`, сплошная сверка чисел валидатором);
* `mcp_architecture.md` — 1 786 строк, из них раздел 9 дублирует докстринги,
  которые теперь живут в коде;
* `agent_architecture.md` — актуален, но описывает часть решений как будущие,
  хотя они реализованы.

**Предлагаемая правка.** Свести к трём документам: данные, инструменты, агент.
Убрать дублирование спецификаций с докстрингами. Добавить схемы потоков данных
и диаграмму слоёв. Отражать реализованное состояние, а не намерения.

---

### 13. Решение по поставщику LLM

**Суть.** Бесплатный ключ Groq даёт 8 000 токенов в минуту и 200 000 в сутки.
Из этого следуют два ограничения, уже проявившиеся на практике:

* второй вопрос в сессии ждёт освобождения минутного окна — замерено 57 секунд;
* платный профиль нельзя ни включить, ни проверить (запись 8);
* суточного лимита хватает примерно на 50–60 полных разборов на всю команду.

**Рассматривается OpenRouter** как способ получить платный доступ. Решение
не принято, разбор вариантов — в ответе к обсуждению от 4 сентября.

---

## 3. Закрытые записи

Оставлены для истории: что было найдено и чем закончилось.

| Что было | Чем закрыто | Проверено |
|---|---|---|
| **Банкротство не доходит до модели и пользователя** | `legal_status` попал в факт-пакет; `agent/verdicts.py` поднимает вердикт при `severity = critical`; `pipeline._required_mentions` дописывает причину, если модель о ней умолчала | Ответ по МАКСМАРКЕТ называет банкротство при зелёном светофоре |
| **Роли теряли негативные факторы** | В `core/roles.py` добавлена роль `activity` с разделами `okved` и `license`. Все шесть разделов, где встречаются негативные факторы, теперь покрыты ролями | Незакрытыми остались только `relatedComp`, `site`, `filials` — в них негативных факторов нет вовсе |
| **Бюджет контекста исчерпан до первой реплики** | `context_token_budget` поднят до 30 000 и разведён с минутным лимитом: `llm_tpm_limit` вынесен отдельным ключом | Бюджет хода считается от профиля |
| **Белого списка ИНН сессии нет** | Реализован в `agent/dispatcher.py`: проверка перед вызовом инструмента, пополнение из ответов `get_affiliations`, `find_similar_contractors`, `compare_contractors` | Есть тесты в `tests/test_agent_tools.py` |
| **Тип `FactPack` на фронте отстал** | `pending` заменён на `pending_count` и `pending_amount` в `ui/src/types.ts` | Сборка фронта проходит проверку типов |
| **Ответ приходил сплошным текстом** | В схему добавлен `positives`; `key_risks` и `positives` вынесены отдельными полями `ChatResponse` и перестали вклеиваться в `analysis`; `draft_followup_questions` подмешивается на разборе автоматически и в промпт не уходит; в `Workspace.tsx` появился компонент `AssistantMessage` с блоками | Разбор МАКСМАРКЕТ: 6 рисков, 5 сильных сторон, 4 вопроса к контрагенту |
| **Уровень риска не был виден в чате** | Шапка сообщения показывает вердикт бейджем плюс оба светофора. Блоки кладутся в `messages.meta`, поэтому история после перезагрузки рисуется так же | `Работать с осторожностью · Низкий риск · ЗСК зелёный` |
| **Пустой чат прокручивался на 56px** | У `.ai-welcome` было `min-height: 100%`: процент считается от content-box `.ai-messages` и складывался с её паддингами 28px сверху и снизу. Заменено на `flex: 1` | Приветственный экран без полосы прокрутки |
| **Страница уезжала в прокрутку, чат оказывался ниже сгиба** | У `.ai-panel` было `min-height: 680px`, но над ней стоят заголовок, карточки сервисов и 94px отступов — на ноутбучном экране панель выталкивала страницу. Заменено на `min-height: clamp(420px, 60vh, 680px)`, рост по-прежнему даёт `flex: 1` | Панель ужимается на низком экране и растягивается на высоком |
| **Два набора кнопок не помещались, дорогой отсекался постфактум** | `tariff_free_max_buttons: 1` | Потолок честный, а не задним числом |
| **`get_inspections` отдавал 52 надзорных органа** | Топ-5 плюс счётчики `authorities_total` и `authorities_other` | Максимум набора 3 650 → 1 278 токенов, медиана не тронута |
| **Набор «Безопасность» раздувался** | `get_affiliations` с `limit=5`; снятые метки ФНС с формулировками «не найден в реестре…» в промпт не идут, вместо них счётчик `confirmed_absent` | Максимум 3 836 → 2 082 токена |
| **Платный профиль всегда деградировал** | `llm_tpm_limit` разведён на `_free` и `_paid` и стал полем `ExecutionProfile`; бюджет считается от окна профиля | Структура верна; значения совпадают, пока нет платного ключа |
| **В сравнении риски и плюсы шли общей кучей** | Отдельная схема `COMPARE_SCHEMA` с массивом `contractors: [{inn, key_risks, positives}]`; состав и порядок задаёт код по пакетам, не модель | Сравнение трёх: у каждого свои списки |
| **Имена полей протекали в списки** | `agent/sanitize.py` вырезает скобочные группы без кириллицы и приводит «ZSK» к «ЗСК». Промпт получил отрицательные примеры | Все четыре наблюдавшиеся протечки вычищаются, легитимные скобки не трогаются |
| **Формальные оценки попадали в сильные стороны** | Правило вынесено в общую константу и подключено к схемам разбора и сравнения, с перечнем того, что в `positives` идти должно | «Формально низкий риск» из плюсов исчез |
| **Двойная точка в дописке** | `sanitize.join` не ставит разделитель после сокращения | «ИП КАСАТКИН А.В. Это отсутствие информации» |
| **Бюджет считался константами, а отправлялся другой текст** | Вес блока меряется по фактической строке; `_request_tokens` считает по содержимому, а не по JSON-дампу конверта, где кавычки экранируются | Разбор со всеми пятью наборами: **0 превышений на 100 контрагентах** |
| **Константы бюджета устарели** | `tokens_system_prompt` 110 → 1170 (значение осталось от раннего минимального промпта), пакеты пересчитаны по p90, доля бюджета 0,7 → 0,8 | Замеры воспроизводимы |
| **Графики: бэкенд был, интерфейса не было** | Компонент `Charts.tsx` рисует SVG из факт-пакета: выручка сразу, четыре остальных по клику. Набор «Графики» убран из кнопок — ряды в промпт больше не уходят | Ход стал дешевле на 163 токена |
| **Сравнение срывалось на «тяжёлых» наборах** | `prompts.compare_summary()` отбирает в промпт только `matrix`, `differences`, `ranking`, `not_found`. Ключ `items` с compact-пакетами больше не сериализуется | 40 наборов по 2 и по 3 контрагента: **0 превышений бюджета**. Медиана на троих 3 204 вместо 6 766 |
| **Сводная таблица не доходила до интерфейса** | Сводка уходит полем `comparison` и рисуется компонентом `ComparisonTable`: строка — контрагент, колонки — оба светофора, вердикт, выручка, чистые активы, долг к активам, взыскания, негативные факторы | Сравнение трёх контрагентов даёт таблицу на 3 строки и 4 различия |
| **Имена полей протекали в списки** | Правило 11 промпта расширено: имена полей допустимы только в `analysis`, в `answer`, `key_risks` и `positives` запрещены | В прогоне после правки протечек нет |
